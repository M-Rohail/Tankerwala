import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand — deep water blue
  static const primary = Color(0xFF0A3D6B);
  static const primaryMid = Color(0xFF1565C0);
  static const primaryLight = Color(0xFF1E88E5);
  static const accent = Color(0xFF00B4D8);
  static const accentLight = Color(0xFF90E0EF);

  // Backgrounds
  static const background = Color(0xFFF0F6FF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceSecondary = Color(0xFFE8F1FD);
  static const cardShadowColor = Color(0x1A0A3D6B);

  // Decorative water ripple tones
  static const ripple1 = Color(0xFFCAE9FF);
  static const ripple2 = Color(0xFF90E0EF);

  // Text
  static const textPrimary = Color(0xFF0D1B2A);
  static const textSecondary = Color(0xFF4A6A8A);
  static const textHint = Color(0xFFADC4D8);
  static const textOnDark = Color(0xFFFFFFFF);
  static const textOnDarkMuted = Color(0xCCFFFFFF);

  // Role colors
  static const tankerBlue = Color(0xFF0A3D6B);
  static const customerTeal = Color(0xFF006D77);

  // Semantic
  static const success = Color(0xFF2D9E6B);
  static const error = Color(0xFFD64045);
  static const warning = Color(0xFFF4A261);

  // Border
  static const border = Color(0xFFD6E4F0);
  static const borderFocus = Color(0xFF1565C0);
}